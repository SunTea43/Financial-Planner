<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card">
      <div class="card-body text-center">
        <h1 class="card-title mb-4">Financial Planner</h1>
        <p class="card-text mb-4">Organiza tus finanzas de manera eficiente</p>
        <% if user_signed_in? %>
          <%= link_to "Ir al Dashboard", authenticated_root_path, class: "btn btn-primary btn-lg" %>
        <% else %>
          <%= link_to "Iniciar Sesión", new_user_session_path, class: "btn btn-primary btn-lg me-2" %>
          <%= link_to "Registrarse", new_user_registration_path, class: "btn btn-outline-primary btn-lg" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
